{
    "encoder_decoder": {
        "name": "cae",
        "params": {
            "hidden_channels": 16,
            "latent_channels": 8,
            "padding_type": [
                "circular",
                "replicate"
            ]
        }
    },
    "latent_dynamics": {
        "name": "lstm",
        "params": {
            "hidden_dim": 1024
        }
    },
    "training": {
        "lr_ed": 1e-4,
        "lr_ld": 1e-2,
        "nepochs": 20000,
        "batch_size": 32,
        "warm_start": true
    }
}