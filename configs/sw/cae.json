{
    "encoder_decoder": {
        "name": "cae",
        "params": {
            "hidden_channels": 16,
            "latent_channels": 8,
            "padding_type": [
                "circular",
                "replicate"
            ]
        }
    },
    "training": {
        "lr_ed": 1e-3,
        "nepochs": 10000,
        "batch_size": 64
    }
}